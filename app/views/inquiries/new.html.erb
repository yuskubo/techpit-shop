<main role="main">
  <div class="container mt-5 p-lg-5">
    <div class="row">
      <div class="col-6">
        <h3>まずは資料請求！</h3>
        <p>
          Techpit Campでは、現役エンジニアが講師を勤めているため、単なるプログラミングスキルだけではなく、
          システム開発の考え方や実践的なノウハウを身に付けることができます。
        </p>
        <small class="text-muted">お申込後、数日でパンフレットをお届けします。</small>
      </div>
      <div class="col-6 bg-light">
        <%= form_with(model: @inquire, local: true, class: "form-signin") do |f| %>
          <div class="form-group">
            <%= f.label :name, "お名前"%>
            <%= f.text_field :name, autofocus: true, class: "form-control" %>
          </div>

          <div class="form-group">
            <%= f.label :email, "メールアドレス" %>
            <%= f.email_field :email, class: "form-control" %>
          </div>

          <div class="form-group form-check">
            <%= f.check_box :disclaimer, { class: " form-check-input" }, true, false %>
            <%= f.label :disclaimer, "プライバシーポリシーに同意します。", class: "form-check-label" %>
            <small>(<%= link_to "https://techpitcamp.co.jp/privacy/", "#" %>)</small>
          </div>

          <div class="d-flex justify-content-center">
          <%= f.submit "資料請求", class: "btn btn-primary btn-lg w-50" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</main>
